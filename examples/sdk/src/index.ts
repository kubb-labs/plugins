/**
 * PetStore SDK entry-point — re-exports the auto-generated SDK facade class.
 *
 * The `PetStoreSDK` class is generated by Kubb via the `sdk` option in
 * `kubb.config.ts`. It follows the MarzbanSDK pattern: one top-level class
 * whose constructor accepts a shared `RequestConfig` and exposes a typed
 * property for every API tag (`petController`, `storeController`, `userController`).
 *
 * Run `pnpm generate` to regenerate `./src/gen/` from the OpenAPI spec.
 *
 * @example
 * ```ts
 * import { PetStoreSDK } from './index.ts'
 *
 * const sdk = new PetStoreSDK({ baseURL: 'https://petstore3.swagger.io/api/v3' })
 *
 * const pets  = await sdk.petController.findPetsByStatus({ status: 'available' })
 * const order = await sdk.storeController.placeOrder({ petId: 1, quantity: 2, status: 'placed' })
 * const user  = await sdk.userController.getUserByName({ username: 'johndoe' })
 * ```
 */

export { PetStoreSDK } from './gen/sdk/petStoreSDK.ts'
