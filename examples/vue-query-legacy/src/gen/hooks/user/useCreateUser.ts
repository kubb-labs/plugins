/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '../../.kubb/client.ts'
import type { CreateUserRequestConfig, CreateUserData, CreateUserResponse } from '../../models/user/CreateUser.ts'
import type { MutationObserverOptions, QueryClient } from '@tanstack/vue-query'
import { client } from '../../.kubb/client.ts'
import { useMutation } from '@tanstack/vue-query'

export const createUserMutationKey = () => [{ url: '/user' }] as const

/**
 * @description This can only be done by the logged in user.
 * @summary Create user
 * {@link /user}
 */
export async function createUser(
  { body }: CreateUserRequestConfig,
  config: Partial<RequestConfig<CreateUserData>> & {
    client?: Client
    contentType?: 'application/json' | 'application/xml' | 'application/x-www-form-urlencoded'
  } = {},
) {
  const { client: request = client, contentType = 'application/json', ...requestConfig } = config

  const requestBody = body

  const res = await request<CreateUserResponse, ResponseErrorConfig<Error>, CreateUserData>({
    method: 'POST',
    url: `/user`,
    body: requestBody,
    contentType,
    ...requestConfig,
  })

  return res.data
}

/**
 * @description This can only be done by the logged in user.
 * @summary Create user
 * {@link /user}
 */
export function useCreateUser<TContext>(
  options: {
    mutation?: MutationObserverOptions<CreateUserResponse, ResponseErrorConfig<Error>, CreateUserRequestConfig, TContext> & { client?: QueryClient }
    client?: Partial<RequestConfig<CreateUserData>> & {
      client?: Client
      contentType?: 'application/json' | 'application/xml' | 'application/x-www-form-urlencoded'
    }
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...mutationOptions } = mutation
  const mutationKey = mutationOptions?.mutationKey ?? createUserMutationKey()

  return useMutation<CreateUserResponse, ResponseErrorConfig<Error>, CreateUserRequestConfig, TContext>(
    {
      mutationFn: async ({ body }) => {
        return createUser({ body }, config)
      },
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  )
}
