/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { Client, RequestConfig, ResponseErrorConfig } from '../../.kubb/client.ts'
import type { UpdateUserRequestConfig, UpdateUserData, UpdateUserResponse } from '../../models/user/UpdateUser.ts'
import type { MutationObserverOptions, QueryClient } from '@tanstack/vue-query'
import { client } from '../../.kubb/client.ts'
import { useMutation } from '@tanstack/vue-query'

export const updateUserMutationKey = () => [{ url: '/user/:username' }] as const

/**
 * @description This can only be done by the logged in user.
 * @summary Update user
 * {@link /user/:username}
 */
export async function updateUser(
  { path, body }: UpdateUserRequestConfig,
  config: Partial<RequestConfig<UpdateUserData>> & {
    client?: Client
    contentType?: 'application/json' | 'application/xml' | 'application/x-www-form-urlencoded'
  } = {},
) {
  const { client: request = client, contentType = 'application/json', ...requestConfig } = config

  const requestBody = body

  const res = await request<UpdateUserResponse, ResponseErrorConfig<Error>, UpdateUserData>({
    method: 'PUT',
    url: `/user/${path.username}`,
    body: requestBody,
    contentType,
    ...requestConfig,
  })

  return res.data
}

/**
 * @description This can only be done by the logged in user.
 * @summary Update user
 * {@link /user/:username}
 */
export function useUpdateUser<TContext>(
  options: {
    mutation?: MutationObserverOptions<UpdateUserResponse, ResponseErrorConfig<Error>, UpdateUserRequestConfig, TContext> & { client?: QueryClient }
    client?: Partial<RequestConfig<UpdateUserData>> & {
      client?: Client
      contentType?: 'application/json' | 'application/xml' | 'application/x-www-form-urlencoded'
    }
  } = {},
) {
  const { mutation = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...mutationOptions } = mutation
  const mutationKey = mutationOptions?.mutationKey ?? updateUserMutationKey()

  return useMutation<UpdateUserResponse, ResponseErrorConfig<Error>, UpdateUserRequestConfig, TContext>(
    {
      mutationFn: async ({ path, body }) => {
        return updateUser({ path, body }, config)
      },
      mutationKey,
      ...mutationOptions,
    },
    queryClient,
  )
}
