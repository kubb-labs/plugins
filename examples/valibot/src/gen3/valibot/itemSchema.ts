/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as v from "valibot";
import { itemTypeASchema } from "./itemTypeASchema.ts";
import { itemTypeBSchema } from "./itemTypeBSchema.ts";

export const itemSchema = v.intersect([v.union([v.intersect([itemTypeASchema, v.object({
    "type": v.picklist(["typeA"])
    })]), v.intersect([itemTypeBSchema, v.object({
    "type": v.picklist(["typeB"])
    })])]), v.object({
    "id": v.optional(v.pipe(v.number(), v.integer())),
    "type": v.picklist(["typeA", "typeB"]),
    "name": v.string(),
    "createdAt": v.optional(v.pipe(v.string(), v.isoDateTime()))
    })])