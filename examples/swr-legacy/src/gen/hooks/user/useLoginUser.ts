/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import useSWR from 'swr'
import type { Client, RequestConfig, ResponseErrorConfig } from '../../.kubb/client.ts'
import type { LoginUserRequestConfig, LoginUserResponse, LoginUserStatus200, LoginUserStatus400 } from '../../models/user/LoginUser.ts'
import type { SWRConfiguration } from 'swr'
import { client } from '../../.kubb/client.ts'

export const loginUserQueryKey = ({ query }: Omit<LoginUserRequestConfig, 'headers'> = {}) => [{ url: '/user/login' }, ...(query ? [query] : [])] as const

type LoginUserQueryKey = ReturnType<typeof loginUserQueryKey>

/**
 * @summary Logs user into the system
 * {@link /user/login}
 */
export async function loginUser({ query }: LoginUserRequestConfig = {}, config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = client, ...requestConfig } = config

  const res = await request<LoginUserStatus200, ResponseErrorConfig<LoginUserStatus400>, unknown>({ method: 'GET', url: `/user/login`, query, ...requestConfig })

  return res.data
}

export function loginUserQueryOptions({ query }: LoginUserRequestConfig = {}, config: Partial<RequestConfig> & { client?: Client } = {}) {
  return {
    fetcher: async () => {
      return loginUser({ query }, config)
    },
  }
}

/**
 * @summary Logs user into the system
 * {@link /user/login}
 */
export function useLoginUser({ query }: LoginUserRequestConfig = {}, options: {
  query?: SWRConfiguration<LoginUserResponse, ResponseErrorConfig<LoginUserStatus400>>,
  client?: Partial<RequestConfig> & { client?: Client },
  shouldFetch?: boolean,
  immutable?: boolean
} = {}) {
  const { query: queryOptions, client: config = {}, shouldFetch = true, immutable } = options ?? {}

  const queryKey = loginUserQueryKey({ query })

  return useSWR<LoginUserResponse, ResponseErrorConfig<LoginUserStatus400>, LoginUserQueryKey | null>(
   shouldFetch ? queryKey : null,
   {
     ...loginUserQueryOptions({ query }, config),
     ...(immutable ? {
         revalidateIfStale: false,
         revalidateOnFocus: false,
         revalidateOnReconnect: false
       } : { }),
     ...queryOptions,
   }
  )
}