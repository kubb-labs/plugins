/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import * as v from "valibot";
import { categorySchema } from "./categorySchema.ts";
import { tagSchema } from "./tagSchema.ts";

export const petSchema = v.object({
    "id": v.optional(v.pipe(v.number(), v.integer())),
    "name": v.string(),
    "log": v.optional(v.pipe(v.string(), v.minLength(2), v.maxLength(42), v.regex(/^[A-Za-z0-9()\[\]'"][-A-Za-z0-9_. \/()\[\]]{0,40}[A-Za-z0-9()\[\]'"]$/))),
    "category": v.optional(categorySchema),
    "photoUrls": v.array(v.string()),
    "tags": v.optional(v.array(tagSchema)),
    "status": v.pipe(v.optional(v.picklist(["available", "pending", "sold"])), v.description("pet status in the store"))
    })

export type PetSchema = v.InferOutput<typeof petSchema>