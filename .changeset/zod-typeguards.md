---
'@kubb/plugin-zod': minor
---

Add `typeGuards` option to generate TypeScript type guards (`is*`) and assertion functions (`assert*`) using Zod v4's native `validate` API (requires Zod v4.6.0 or above).
