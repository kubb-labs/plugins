---
---

Update example configs to use `barrelType: 'named'` for most plugins, and `barrelType: 'all'` for the client example. The mcp example keeps `barrelType: 'all'` to remain compatible with `verbatimModuleSyntax`.
