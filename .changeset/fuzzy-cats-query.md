---
'@kubb/plugin-react-query': patch
'@kubb/plugin-vue-query': patch
'@kubb/plugin-swr': patch
---

Allow mutation generation when query generation is disabled, keeping the `query` and `mutation` options independent.
