---
"@kubb/plugin-ts": minor
---

Add type generator v2 helpers:

- `buildDataSchemaNode` — builds request body schema node
- `buildParamsSchema` — builds grouped parameter schema node
- `buildResponsesSchemaNode` — builds responses wrapper schema node
- `buildResponseUnionSchemaNode` — builds response union schema node

Add constants: `OPTIONAL_ADDS_UNDEFINED`, `OPTIONAL_ADDS_QUESTION_TOKEN`, `ENUM_TYPES_WITH_KEY_SUFFIX`.
